<Page x:Class="AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="AdminPage">
    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.ControlColorKey}}"/>
        </Grid.Background>
        <TreeView x:Name="employeeTree" HorizontalAlignment="Left" Margin="10,10,0,10" Width="126">
            <TreeView.ItemContainerStyle>
                <Style TargetType="{x:Type TreeViewItem}">
                    <Setter Property="IsExpanded" Value="True"/>
                </Style>
            </TreeView.ItemContainerStyle>
            <TreeViewItem Header="Admin">
                <TreeViewItem x:Name="treeDept" Header="Departments"/>
                <TreeViewItem x:Name="treeDesg" Header="Designations"/>
                <!--<TreeViewItem x:Name="treeSalG" Header="Salary Grades"/>-->
            </TreeViewItem>
        </TreeView>

        <Grid Margin="141,10,10,10">
            <Frame x:Name="adminFrame" Content="" NavigationUIVisibility="Hidden" Focusable="False"/>
        </Grid>

    </Grid>
</Page>
